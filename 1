# Initialize Git
git init

# Connect to GitHub
git remote add origin https://github.com/Narender-731/my-first-project.git

# Add and commit files
git add .
git commit -m "Initial commit"

# Push code to GitHub
git branch -M main
git push -u origin main
